# Notebooks

Use this directory for exploratory research notebooks, feature experiments, and one-off investigations that should stay separate from the reusable platform modules.

Suggested conventions:

- Keep notebooks focused on a single research question.
- Promote stable logic into `app/` or `strategies/` once it becomes reusable.
- Avoid storing large raw datasets in the repository.
